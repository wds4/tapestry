# Secure Relay Key Storage Configuration
# Copy this file to .env and configure for your deployment

# Storage backend options: encrypted-file, sqlite, vault
RELAY_KEY_STORAGE=encrypted-file

# Master key for encryption (REQUIRED)
# Generate with: openssl rand -hex 32
RELAY_KEY_MASTER_KEY=your_32_byte_hex_key_here

# Storage path for encrypted files (default: /var/lib/brainstorm/secure-keys)
RELAY_KEY_STORAGE_PATH=/var/lib/brainstorm/secure-keys

# SQLite options (if using sqlite storage)
RELAY_KEY_DB_PATH=/var/lib/brainstorm/relay-keys.db

# Vault options (if using vault storage)
VAULT_ENDPOINT=http://localhost:8200
VAULT_TOKEN=your_vault_token_here
VAULT_MOUNT_PATH=secret
