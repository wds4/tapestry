/**
 * GrapeRank Queries index file
 * Exports all graperank query-related handlers
 */

const { handleGetInfluenceCount } = require('./influence-count');

module.exports = {
    handleGetInfluenceCount
};
